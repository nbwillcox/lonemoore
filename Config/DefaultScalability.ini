; Keep native output resolution. Avoid the 200% (4x pixel) TSR history buffer
; on the normal Epic preset; Cinematic retains the engine's 200% history option.
[AntiAliasingQuality@3]
r.TSR.History.ScreenPercentage=100
